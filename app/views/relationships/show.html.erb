<p id="notice"><%= notice %></p>

<table class="table">
  <thead>
    <tr>
      <th>User</th>
      <th>First Name</th>
      <th>Last Name</th>
      <th>Email</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% if @users_roles.user == @relationship.user_id %>
      <tr>
        <td><%= @users_roles.role %></td>
        <td><%= @users_roles.first %></td>
        <td><%= @users_roles.last %></td>
        <td><%= @users_roles.email %></td>
        <td><%= link_to 'Edit', edit_relationship_path(relationship) %></td>
        <td><%= link_to 'Destroy', relationship, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Edit', edit_relationship_path(@relationship) %> |
<%= link_to 'Back', relationships_path %>
