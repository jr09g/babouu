<%= form_for(@users_group) do |f| %>
  <% if @users_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@users_group.errors.count, "error") %> prohibited this users_group from being saved:</h2>

      <ul>
      <% @users_group.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%#= f.label :group_id %><br>
    <%#= f.number_field :group_id %>
  </div>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.collection_select(:user_id , @business_users, :user_id, :first_name) %>
  </div>

  <div class="field">
    <%= f.label :group_id %><br>
    <%= f.collection_select(:group_id , @business_groups, :id, :name) %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
