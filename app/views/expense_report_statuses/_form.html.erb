<%= form_for(@expense_report_status) do |f| %>
  <% if @expense_report_status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@expense_report_status.errors.count, "error") %> prohibited this expense_report_status from being saved:</h2>

      <ul>
      <% @expense_report_status.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
